# Style guide

> The shared Julia style lives in `~/Packages/CLAUDE.md` ("Julia style"):
> official Julia + DFTK guides, naming, `for i = 1:n` vs `for x in xs`,
> explicit named tuples, ≤ 92 cols, hot-path `SVector`/`MVector`/`@views`/
> `@inbounds`. List only **package-specific additions or deviations** here.

## Argument order

> If this package fixes a constructor/verb argument shape beyond the
> language-level ordering, document it here. Otherwise delete this section.

## Docstrings

> Note any package-specific docstring shape (sections, ordering). Otherwise the
> standard Julia format applies.

## Package-specific naming

> Domain terms, type-name conventions, or reserved names. Otherwise delete.
